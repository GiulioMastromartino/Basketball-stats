[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "basketball-stats-rust"
version = "0.1.0"
description = "High-performance basketball analytics in Rust"
requires-python = ">=3.8"
dependencies = []

[tool.maturin]
rustfmt = true
